# Core dependencies for CSV/JSON processing
pandas>=1.5.0

# HTTP requests for EDH API downloads
requests>=2.28.0

# Entity extraction uses fast regex-based pattern matching
# No ML dependencies required for basic pattern-based parser

# Optional: Hybrid grammar parser with morphological analysis and dependency parsing
# Uncomment the line below to enable advanced grammatical parsing with CLTK
# cltk>=1.5.0
